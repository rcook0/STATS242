[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stats242xtb"
version = "0.6.0"
description = "STATS 242 executable textbook runners (model cards + adapters + tests)"
requires-python = ">=3.10"

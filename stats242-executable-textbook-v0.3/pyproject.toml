[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "stats242xtb"
version = "0.3.0"
description = "STATS 242 executable textbook runners (filled chapter set)"
requires-python = ">=3.10"
